apiVersion: v1
kind: Service
metadata:
  name: docling-serve
  labels:
    app: docling-serve
spec:
  selector:
    app: docling-serve
    component: docling-serve-api
  ports:
    - name: http
      port: 5001
      targetPort: http
      protocol: TCP
